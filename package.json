{
  "name": "growthhub-deploy",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "postinstall": "npm ci --prefix growthhub",
    "build": "npm run build --prefix growthhub",
    "start": "npx --yes serve growthhub/dist -s -l tcp://0.0.0.0:${PORT:-3000}"
  }
}
